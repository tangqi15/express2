// admin 模块的业务逻辑层

module.exports.loginService = async function (loginData) {
    console.log(loginData, 'loginData                 11111111111');
    
    // 调用 adminModel 模块，进行数据库操作
    // const result = await adminModel.login(loginData);
    // return result;
}